import { CommandCenter } from "@/components/CommandCenter";

/** Same experience as `/` — kept for bookmarks and external links. */
export default function DashboardPage() {
  return <CommandCenter />;
}
